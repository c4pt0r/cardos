#pragma once
// Device-side secrets. Copy this file to Secrets.h (gitignored) and fill in
// the real values. Secrets.h must never be committed.

namespace secrets {

// Shared key the Voice Memo app sends as X-Upload-Key to the cardos-voice
// Worker. Must match the Worker's UPLOAD_KEY secret (synced from GitHub
// Secrets by the deploy workflow).
constexpr const char* kUploadKey = "REPLACE_ME";

}  // namespace secrets
